# Phase 4 only serves the UI. API keys live in Phase3/.env (Groq).

# Phase 3 API URL (used by the browser — set in config.js or ?api= query)
PHASE3_API_BASE=http://127.0.0.1:8001

# Phase 4 UI server
UI_HOST=127.0.0.1
UI_PORT=8080
